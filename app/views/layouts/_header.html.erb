<header>
  <nav>
    <div class="home">
      <%= link_to "Daiki Ikeda", root_url %>
    </div>
    <ul>
    <% Category.all.each do |category| %>
      <li><%= link_to category.name, category_name_path(category) %></li>
    <% end %>
    </ul>
    <div class="clearfix"></div>
  </nav>
</header>
